version: "3.8"

services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    image: main
    ports:
      - "8080:8080"

  redis:
    build:
      context: ./docker-files
      dockerfile: redis
    image: redis
    ports:
      - "6379:6379"

  nginx:
    build:
      context: ./docker-files
      dockerfile: nginx
    image: nginx
    ports:
      - "80:80"
