version: "3.8"

services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
    image: main:latest
    ports:
      - "8080:8080"

  redis:
    build:
      context: ./docker-files
      dockerfile: redis
    image: redis:latest
    ports:
      - "6379:6379"

  nginx:
    build:
      context: ./docker-files
      dockerfile: nginx
    image: nginx:latest
    ports:
      - "80:80"
