spring:
  profiles:
    active:
      - prod
    group:
      local:
        - local
      prod:
        - prod
    include:
      - kafka
      - redis
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html